import { Box, Container, Typography } from '@mui/material';

import { DataGrid } from 'components/DataGrid';

const Home: React.FC = () => {
  return (
    <Container>
      <DataGrid />
    </Container>
  );
};

export default Home;
